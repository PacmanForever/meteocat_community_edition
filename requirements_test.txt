# Test requirements for Meteocat (Community Edition)
# Note: Full Home Assistant tests don't work on Windows due to fcntl dependency
# These are basic unit tests for the API client
pytest>=7.4.0
pytest-asyncio>=0.21.0
aiohttp>=3.9.0
